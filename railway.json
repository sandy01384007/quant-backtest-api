{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn api_server:app --host 0.0.0.0 --port 8000",
    "numReplicas": 1,
    "sleepApplication": true,
    "restartPolicyMaxRetries": 10
  }
}